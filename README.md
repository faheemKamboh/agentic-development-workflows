# Development Workflow Lab

Small, reproducible workflow experiments for evaluating local/open-weight models in CI environments.

The repository intentionally contains only generic execution tooling and public benchmark interfaces. Workflows are manual-only by default.
